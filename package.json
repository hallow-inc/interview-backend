{
  "name": "interview-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:start": "sh start_database.sh",
    "db:stop": "docker stop database && docker rm database",
    "db:destroy": "npm run db:stop && docker image rm interview-db:latest",
    "db:reset": "npm run db:stop && npm run db:start",
    "setup": "npm install && npm run db:start",
    "start": "node -r dotenv/config index.js"
  },
  "author": "hallow",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0",
    "dotenv": "^17.2.3",
    "knex": "^0.95.5",
    "moment": "^2.29.4",
    "mysql2": "^2.2.5"
  }
}
